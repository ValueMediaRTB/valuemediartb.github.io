export const config = {
  reportId: 10, // You can change this to your desired report ID
  // Add other configuration options here as needed
  version: "1.0.0",
  appName: "TrafficTools",
  serverURL:"http://localhost:3000"
};

export default config;